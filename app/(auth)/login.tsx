import { View, Text } from 'react-native';

export default function LoginScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#08103d',
      alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#f5c518', fontSize: 24 }}>
        Pinchanted ✨
      </Text>
    </View>
  );
}